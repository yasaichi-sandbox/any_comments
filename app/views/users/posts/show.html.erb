<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @post.body %>
</p>

<%= render 'shared/comment/form',
      comment: @new_comment,
      url: post_comments_path(@post)
%>

<%= render 'shared/comment/list', comments: @post.comments %>

<%= link_to 'Back', user_posts_path(@post.user) %>
